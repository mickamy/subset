// Package tsql provides SQL helpers shared by integration tests.
// Helpers live in build-tagged files; this stub keeps the package visible
// to non-integration builds (e.g., `go test ./...`).
package tsql
